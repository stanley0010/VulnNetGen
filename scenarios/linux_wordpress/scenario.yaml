---
# Default scenario file
name: Linux wordpress
description: vulnerable wordpress with weak admin password
author: RIC1
difficulty: easy
systems:
  - name: wordpress
    base: ubuntu22
    ip: 192.168.77.8
    accounts:
      - name: root
        password: root_user_password
        privileged: true
      - name: www-data
        password: regular_user_password
        privileged: false
    vulnerabilities:
      - name: "linux/clean_wordpress"
    generators:
      - name: flag
        path: ../components/generators/flag
        args:
          flag: flag{linux_wordpress_weak_admin_password}
          path: /flag.txt
          owner: root
          group: root
          mode: 0700
